{
  "type": "module",
  "dependencies": {
    "preact": "^10.7.1",
    "purecss": "^2.1.0"
  },
  "devDependencies": {
    "@types/w3c-web-serial": "^1.0.2",
    "esbuild": "^0.14.38",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "build": "esbuild src/app.tsx src/app.css --bundle --sourcemap --outdir=site",
    "check": "tsc --noEmit",
    "dev": "npm run build -- --servedir=site"
  }
}
